import 'package:flutter/material.dart';

class AllTaskViewModel extends ChangeNotifier {
  // Task data is managed by TaskViewModel (shared via Provider)
  // This ViewModel handles only AllTask screen-specific UI state
}
